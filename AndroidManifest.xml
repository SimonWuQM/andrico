<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="org.andrico.andrico"
      android:versionCode="1" android:versionName="0.6.0">
        
    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name=".MainActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".Synchronize"
        		  android:label="@string/synchronize">
        </activity>
        
        <activity android:name=".StartSynchronization"
        		  android:label="@string/synchronize">
        </activity>
        
        
        <activity
			android:name=".facebook.LoginActivity"
			android:label="@string/login_label"
			
			android:configChanges="keyboardHidden|orientation">
		</activity>

		<activity
			android:name=".facebook.ExtendedPreferenceActivity"
			android:label="Authorize Facebook Extended Preference">
		</activity>

			
    </application>
    <uses-permission android:name="android.permission.service.blogger"/>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest> 